# CLAUDE.md（永続仕様）

## プロジェクト概要

**プロジェクト名**: とよだ謎とき（なぞとき探偵トヨタ）
**コンセプト**: 見習い探偵トヨタくんが 1 問ずつ問題を出し、正解したら次に進む謎解きアプリ。3 つのキーワードから最終問題の答えを導く。
**ターゲット**: 地域の子ども〜大人（謎解き初心者中心）
**プラットフォーム**: Web（スマートフォン優先）

---

## 技術スタック

- **フレームワーク**: Next.js 15 (App Router)
- **言語**: TypeScript
- **UI**: Tailwind CSS v4 + shadcn/ui 準拠のコンポーネント
- **状態管理**: Zustand（persist ミドルウェアで localStorage 保存）
- **音声**: Web Speech API（`/lib/speech.ts`）
- **テスト**: Vitest

### v0 生成コードとの関係（2026-08-16 更新）

v0 が実際に生成したのは **Cloudscape Design System** 版だった。
CLAUDE.md 初版の想定（Tailwind + shadcn）と食い違っていたため、
以下を引き継いだうえで Tailwind に作り直した。

- 引き継いだもの: 問題データ、図のレイアウト、キャラクター画像、画面の流れ
- 作り直した理由: v0 の標準出力が Tailwind + shadcn であり、今後 v0 でデザインを回すため

`/components/v0` ディレクトリは使わない（Cloudscape 版とは混在できないため）。

---

## デザイン方針

- **キャラクター**: 見習い探偵「トヨタくん」（明るい、責めない、一緒に考える）
- **トーン**: 明るく親しみやすい。ひらがな多め、漢字にはふりがな相当の言い換えを添える
- **配色**:
  - Primary: #3B82F6（青）
  - Success: #10B981（緑）
  - Warning: #F59E0B（オレンジ）
  - Background: #F8FAFC（薄いグレー）
- **フォント**: Noto Sans JP
- **画面サイズ**: スマートフォン（375px〜428px）を優先。`max-w-md` で中央寄せ
- **タップ領域**: ボタンは高さ 48px 以上、入力欄の文字は 16px 以上（iOS の自動ズーム防止）

---

## ゲームフロー

```
イントロ（キャラクター登場・目的提示）
 ↓ 第1問：漢字＋矢印　　　　→ キーワード「さかえく」
 ↓ 第2問：暗号グリッド　　　→ キーワード「あき」
 ↓ 第3問：ことば＋分数　　　→ キーワード「はな」
 ↓ 最終問題：3つのキーワードから答えを導く → 「きく（菊）」
 ↓ 結果（星評価・ヒント使用数）
 ↓ 「もう一度あそぶ」
```

---

## 問題データ構造

`/types/game.ts` を参照。要点のみ:

- `Puzzle` は `id` / `hints`（必ず 3 つ）/ `answers`（表記ゆれを列挙）/ `keyword` / `speech` を持つ
- `PuzzleVisual` は `kind` による判別ユニオン。新しい出題形式は kind を 1 つ増やす
- `nearMiss` は「途中までは合っている答え」への専用コメント（任意）。
  正しく解けている途中経過を、ただの不正解として突き放さないため
- `speech` は読み上げ用の文面。`question` には「↑」「3/4」など読み上げに向かない記号が入るため分離した

---

## 実装方針

### 画面構成

- `/` : ゲーム画面（イントロ〜結果まで 1 ページ内で遷移）

画面遷移を URL で分けていないのは、進捗が Zustand にあり、
ページ遷移するとブラウザバックで状態が壊れやすいため。

### 回答判定（`/lib/answer-checker.ts`）

- NFKC 正規化で全角英数 → 半角、半角カナ → 全角カナ
- カタカナ → ひらがなに統一
- 空白・句読点・記号を除去
- **長音記号「ー」は残す**（消すと「とうきょう」と「とーきょー」を潰すため）
- 漢字は読みへ自動変換できないので、漢字表記は `answers` 配列に列挙する
- `mode: "exact" | "partial"` を設定で切り替え可能

### ヒントシステム

- 各問題に 3 段階。段階が上がるほど答えに近づく
- 使用数は問題 ID ごとに記録し、結果画面の星評価に反映
- 一度開いたヒントは閉じない

### 星評価

- ヒント 0 回 → 星 3「名たんてい」
- 1〜4 回 → 星 2「一人前のじょしゅ」
- 5 回以上 → 星 1「みならいじょしゅ」
- 最低でも星 1 は残す（最後まで解いた人が惨めにならないため）

### 進行制御

- 正解するまで次へ進めない（`goNext` が `solved` を見て弾く）
- 不正解時は同じ画面に留まり、キャラクターのコメントを 3 種類でローテーション
- 進捗は localStorage に自動保存。リロードしても続きから

---

## コーディング規約

- **関数名**: camelCase
- **コンポーネント名**: PascalCase
- **ファイル名**: kebab-case
- **ディレクトリ構成**: `/app` `/components` `/components/ui` `/lib` `/data` `/hooks` `/stores` `/types`
- **コメント**: 日本語、簡潔に、なぜその実装を選んだかを重視

---

## 拡張性

- **問題の追加・変更**: `/data/puzzles.ts` のみ編集する。id 重複禁止、hints は 3 つ、answers に漢字表記も入れる
- **複数ステージ**: `puzzleSets` 配列に追加。localStorage キーはセット ID を含むので混ざらない
- **音声**: `/lib/speech.ts`。読み上げ文面は各問題の `speech` に持たせる
- **問題編集画面**: 将来つくる場合は `/data/puzzles.ts` を JSON 化し、管理画面から読み書きする形にする

---

## 禁止事項

- 問題文や正解をコードに直接ハードコードしない（必ず `/data/puzzles.ts` から読み込む）
- 状態管理を複数の場所で行わない（Zustand に統一）
- 正解判定を AI に任せない（ルールベースで実装）
- アクセス制限を設けない（URL を知っている人に遊んでもらう前提。
  フロント側だけの合言葉は保護にならないため、置かない）
