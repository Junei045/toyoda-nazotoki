# 現在の進捗状況

**最終更新**: 2026-08-16
**セッション番号**: 11

---

## 完了タスク

- [x] CLAUDE.md の作成・実態に合わせて更新
- [x] v0 生成コードの取り込み（Cloudscape 版から問題データ・図・画像・画面の流れを移植）
- [x] 問題データ構造の定義（`/types/game.ts`）
- [x] PDF「とよだ謎とき」の問題データを実装（`/data/puzzles.ts`）
- [x] 回答判定の表記ゆれ対応（`/lib/answer-checker.ts`）
- [x] Zustand による状態管理（`/stores/game-store.ts`）
- [x] localStorage による進捗保存（persist ミドルウェア）
- [x] ヒント 3 段階の実装
- [x] 星評価（結果画面）の実装
- [x] 音声読み上げ（Web Speech API）
- [x] リセット機能
- [x] PDF 原稿の入手・3問すべての解き筋を確定
- [x] 第1問・第2問のヒントを実際の解き筋に書き直し
- [x] 第1問から「太陽2つ」を削除（原稿の菊の花の誤読だった）
- [x] 第2問に nearMiss（途中までの答え「たかはしあき」への専用コメント）
- [x] 結果画面に菊の花のイラストを追加
- [x] テストコード（36 件・全通過）
- [x] GitHub Pages 公開の設定（静的書き出し・basePath・.nojekyll）
- [x] GitHub Actions のデプロイワークフロー（テストが通らないとデプロイしない）
- [x] `assetPath()` の追加（unoptimized 画像に basePath が付かない問題への対処）
- [x] 合言葉ゲートを削除（URL を知っている人に遊んでもらう方針のため不要と判断）
- [x] 初回 push でテストが落ちた件を修正（jsdom を外し、Actions の Node を 22 に）
- [x] 読み上げの誤読を修正（「表」が「おもて」と読まれていた。読み上げ文をかな寄りに）
- [x] 正解後の読み上げを、問題文ではなく正解メッセージに切り替え
- [x] 最終問題の正解直後に答えを明かさないよう変更（ネタバレは結果画面に集約）
- [x] 結果画面に「なぜ『きく』？」の解説を追加
- [x] 進捗バーの不具合を修正（最終問題が正解しても青のまま、結果画面では灰色に戻っていた）
- [x] 問題を差し替えたとき、古い進捗で画面が壊れる問題に対処（reconcileProgress）
- [x] 進捗バーのテストを問題数に依存しない形に修正
- [x] 問題の追加・差し替えガイド（README-puzzles.md）を作成
- [x] キャラクターのセリフを吹き出しから読み上げられるように（イントロ〜結果まで全場面）
- [x] 読み上げの声の選び方を改善（自然な声を優先して選ぶ）
- [x] 読み上げ文の整形（表示用の空白・改行が不自然な間になっていた）
- [x] `npx next build` 成功を確認

---

## 進行中タスク

なし（動作確認待ち）

---

## 次のタスク（優先度順）

1. GitHub にリポジトリを作って push（README-deploy.md の手順）
2. Settings → Pages → Source を「GitHub Actions」に変更
3. 公開 URL で実機確認（README-setup.md の確認手順）
4. 問題編集画面の検討（`/data/puzzles.ts` の JSON 化）
5. キャラクターの表情差分（現在は 1 枚絵を枠色で出しわけ）
   ※ 読み上げをさらに自然にしたい場合は、セリフを録音した音声ファイルを
     public/audio/ に置き、あれば再生・無ければ合成音声、という作りにできる
6. 効果音（正解・不正解）

---

## 重要なファイル

| ファイル | 内容 |
|---------|------|
| `/data/puzzles.ts` | **問題データ。問題の追加・変更はここだけ** |
| `/README-puzzles.md` | 問題の追加・差し替えガイド |
| `/types/game.ts` | 型定義 |
| `/lib/answer-checker.ts` | 回答判定（表記ゆれ吸収） |
| `/lib/answer-checker.test.ts` | テスト |
| `/lib/speech.ts` | 読み上げ（Web Speech API） |
| `/stores/game-store.ts` | Zustand・進捗保存・星評価 |
| `/hooks/use-hydrated.ts` | localStorage 復元待ち |
| `/components/nazotoki-game.tsx` | ゲーム本体 |
| `/components/puzzle-visual.tsx` | 問題の図（3 種類） |
| `/components/ui/*` | shadcn/ui 準拠のボタン等 |
| `/lib/asset-path.ts` | 画像パスに basePath を付ける |
| `/next.config.ts` | 静的書き出しと basePath の設定 |
| `/.github/workflows/deploy.yml` | GitHub Pages への自動デプロイ |

---

## 技術的決定

- **Cloudscape → Tailwind + shadcn に移行**: v0 の標準出力が Tailwind + shadcn であり、
  今後 v0 でデザインを回すことを優先した。バンドルも 173kB → 126kB に軽くなった
- **判定は正規化してから比較**: NFKC ＋ カタカナ→ひらがな ＋ 空白/記号除去。
  漢字は読みへ自動変換できないので `answers` に列挙する方針
- **長音記号「ー」は消さない**: 消すと「とうきょう」と「とーきょー」を潰すため
- **状態を Zustand に集約**: Cloudscape 版は useState をコンポーネント内に散らしていたため
  リロードで進捗が消えていた。store にまとめて persist をかけて解決
- **localStorage キーに問題セット ID を含める**: 問題を差し替えたとき古い進捗を引きずらないため
- **`useHydrated` で復元待ち**: 復元前に描画すると hydration mismatch になるため
- **画面遷移を URL で分けない**: 進捗が Zustand にあり、ページ遷移でブラウザバックすると壊れやすいため
- **`speech` を `question` と分離**: 問題文には「↑」「3/4」など読み上げに向かない記号があるため
- **音声は自動再生しない**: iOS Safari はユーザー操作がないと発話しないため、必ずボタンから呼ぶ
- **GitHub Pages 向けに静的書き出し**: `output: "export"`。サーバーが無いため
- **basePath を環境変数から取る**: 公開 URL が `/<リポジトリ名>/` の下になる。
  ハードコードするとリポジトリ名を変えたときに壊れるので、
  Actions が `github.event.repository.name` から自動で入れる
- **`assetPath()` を自作**: `images.unoptimized: true` のとき `next/image` は
  basePath を付けない実装になっている（Next.js の get-img-props.js で確認）。
  そのままだとキャラクター画像だけ 404 になるため、自分で付けている
- **テスト環境を jsdom から node に変更**: テストはロジックとデータの検証だけで
  コンポーネントを描画しない。jsdom は Node 22 以上を要求するため、
  Actions の Node 20 でテストが起動できず初回 push が失敗した。
  使わない依存（jsdom / @vitejs/plugin-react）を外して環境要求をゆるくした
- **Actions の Node を 22 に**: Next.js の推奨も 20 系の後半以降。念のため二重の対策
- **デプロイ前にテストを走らせる**: 問題データを壊したまま公開する事故を防ぐため
- **合言葉ゲートを置かない**: URL を知っている人に遊んでもらう方針。
  フロント側だけの合言葉は保護にならず、入口の手間が増えるだけと判断した
- **読み上げ文を `speech` / `successSpeech` に分けてかな寄りで書く**:
  読み上げエンジンは漢字の読みを取りちがえる。「表」は「ひょう」ではなく
  「おもて」と読まれた。読みが割れる語はテストで検出している
- **答えは結果画面でだけ明かす**: 正解した瞬間と結果画面の2回 答えを出していたため、
  最終問題の `success` はネタバレしない文に変え、`ending` を結果画面用に新設した
- **結果画面に解説を置く**: 「きく」が答えである理由（栄区の区の花・秋の代表的な花・花そのもの）
  がどこにも書かれていなかった。3つのキーワードが1つの花を指すことを最後に見せる
- **進捗バーの計算を `getProgress()` に切り出した**: 「解けた数＝獲得キーワード数」で
  数えていたため、キーワードを持たない最終問題がいつまでも未クリア扱いになっていた。
  最終問題のクリアは phase から判定する。コンポーネントの外に出したのはテストで押さえるため
- **読み込み時に進捗を点検する（`reconcileProgress`）**: 問題を差し替えたあと、
  前に遊んだ人のブラウザには古い進捗が残っている。3問を2問に減らすと
  puzzles[2] が存在せず問題カードごと消えるため、合わない進捗は黙って最初からにする。
  問題文だけ直した場合は問題数もIDも変わらないので進捗は保たれる
- **テストを問題数に依存しない書き方にした**: 進捗バーのテストが「3問」前提だったため、
  問題を1つ足すだけで無関係に落ちていた。データから問題数を求める形に修正
- **読み上げボタンを「セリフ」と「問題文」で分けた**: 吹き出し側にボタンを置くことで、
  イントロから結果画面まで、問題文を聞かずにセリフだけを追える
- **声を点数式で選ぶ**: 端末には日本語音声が複数入っていることが多く、
  最初に見つかった声（多くは古い機械音声）を使うとぎこちない。
  Natural / Neural 系、クラウド音声、Google 音声を優先して選ぶ
- **読み上げ前にテキストを整形する**: 表示用に入れた半角スペースや改行が
  そのままだと不自然な間になる。空白は詰め、改行は読点にして読ませる
- **かなにするのは読みが割れる語だけ**: かなだらけにすると、
  読み上げエンジンが抑揚を判断できず かえって不自然になる
- **星は最低 1 を残す**: 最後まで解いた人が惨めにならないようにするため
- **第1問の「太陽2つ」を削除**: v0 が原稿右下の菊の花のイラスト（＝答えの絵）を
  第1問の要素と誤読していた。矢印は「漢字のどの位置にカタカナが隠れているか」を指す
  （花の上=サ、加の左=カ、功の左=エ、久の左上=ク）
- **第2問に nearMiss を用意**: グリッドを消した直後の「たかはしあき」は
  正しく解けている途中経過なので、ただの不正解として突き放さない
- **解き筋自体をテストで検証**: グリッドから め・が・ね・ば を消すと たかはしあき になること、
  分数どおりに文字を取ると はな になることをテストで確認している。
  問題データを変えたときに、解けない問題になっていないかを自動で見つけられる

---

## 現在の課題・疑問点

- [ ] Public リポジトリだと答えがソースから読める。
      地域向けの遊びとしては許容範囲と判断しているが、公開前に一度確認すること
- [ ] 問題を非エンジニアが編集できる形（管理画面 or JSON）は未着手
- [ ] キャラクターの表情差分が無い
- [ ] 読み上げの自然さは端末の音声しだい。決定的に良くするには
      録音した音声ファイルを用意するしかない（下記「次のタスク」参照）

---

## 次回セッション開始時の指示

HANDOFF.md を読み、以下の手順で作業を再開してください。

1. 「進行中タスク」の続きから着手
2. 完了したら「完了タスク」にチェック
3. 新しいタスクを「次のタスク」に追加
4. 技術的決定や課題があれば追記
5. セッション終了時にこのファイルを更新
